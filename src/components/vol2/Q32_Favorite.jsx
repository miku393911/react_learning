import { useState } from "react";

export function Q01_Favorite() {
    // TODO: お気に入り状態を管理するステートを作成
    // ボタンをクリックすると、お気に入りの状態が切り替わる
    // お気に入りの場合は「❤️ お気に入り」、そうでない場合は「🤍 お気に入り」を表示

    return (
        <button>🤍 お気に入り</button>
    );
}
